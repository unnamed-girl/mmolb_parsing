To download and cache all completed games, run:
```
cargo run --bin downloader "path to the download cache"
```

To parse all games in a folder of cached games:
```
cargo run --bin parser "path to the download cache" "path to where events should be saved"
```